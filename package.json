{
  "scripts": {
    "dev": "cd ./server && npm run dev & cd ./client && npm run dev",
    "start-server": "cd ./server && npm install && npm run start",
    "start-client": "cd ./client && npm install && npm run build && npm run start",
    "start": "npm run start-server & npm run start-client"
  },
  "engines": {
    "node": "14.x"
  },
  "cacheDirectories": [
    "node_modules",
    "client/node_modules",
    "server/node_modules"
  ],
  "dependencies": {
    "express": "^4.16.2"
  },
  "devDependencies": {
    "prettier": "2.3.2"
  },
  "prettier": {
    "printWidth": 80,
    "semi": false,
    "trailingComma": "all",
    "singleQuote": true,
    "arrowParens": "avoid"
  }
}
